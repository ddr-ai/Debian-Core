# DainRaku OS — Liquorix kernel repository
# The signing key ships alongside this file as liquorix.key.chroot, which
# live-build installs into the chroot's trusted keyrings BEFORE it runs apt
# update in the bootstrap/archives stage. This lets Liquorix kernel packages
# resolve when processing the base package list.
deb https://liquorix.net/debian stable main
