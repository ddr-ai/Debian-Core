# GitHub repository that hosts the ISO releases (org/repo format).
# The download page uses this to fetch release metadata from the GitHub API.
# Set this to your fork's repo name so the download page points to your releases.
# Example: VITE_GITHUB_REPO=myorg/dainraku
VITE_GITHUB_REPO=dainraku-os/dainraku
